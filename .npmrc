# @wordpress/scripts' webpack/babel config expects a handful of tooling
# packages (eslint, prettier, babel presets) to be resolvable the way npm's
# flat node_modules would resolve them. pnpm's strict, symlinked layout
# breaks that unless hoisting is relaxed.
shamefully-hoist=true
